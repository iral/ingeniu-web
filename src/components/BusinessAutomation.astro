---

---

<div class="automation-showcase reveal">
    <div class="automation-header">
        <div class="window-controls">
            <span class="dot red"></span>
            <span class="dot yellow"></span>
        </div>
        <div class="window-title">
            Ingeniu Workflow Engine: Accounts Payable
        </div>
    </div>

    <div class="automation-body">
        <div class="col-input">
            <div class="card-label">INPUT: CORREO RECIBIDO</div>
            <div class="email-mockup">
                <div class="email-head">
                    <div class="avatar"></div>
                    <div class="meta">
                        <strong>De:</strong> Proveedores SA<br />
                        <strong>Asunto:</strong> Factura #FE-9021
                    </div>
                </div>
                <div class="email-body">
                    Adjunto factura de servicios de cloud...<br />
                    <div class="attachment-file">📄 invoice_9021.pdf</div>
                </div>
            </div>
        </div>

        <div class="process-arrow">➞</div>

        <div class="col-process">
            <div class="card-label">PROCESO AUTOMÁTICO</div>
            <div class="step-list" id="process-steps">
                <div class="step pending" id="step-1">
                    <span class="icon">👁️</span> Leyendo PDF (OCR)
                </div>
                <div class="step pending" id="step-2">
                    <span class="icon">🔍</span> Validando Orden de Compra (PO)
                </div>
                <div class="step pending" id="step-3">
                    <span class="icon">💾</span> Ingreso en ERP (SAP/Oracle)
                </div>
                <div class="step pending" id="step-4">
                    <span class="icon">✅</span> Pago Programado & Notificación
                </div>
            </div>
        </div>
    </div>

    <div class="action-area">
        <button id="trigger-automation" class="btn btn-primary">
            Ejecutar Automatización
        </button>
        <p class="small-note">
            Ahorro estimado: 15 min de trabajo manual por factura.
        </p>
    </div>
</div>

<script>
    const btn = document.getElementById("trigger-automation");
    const steps = [
        document.getElementById("step-1"),
        document.getElementById("step-2"),
        document.getElementById("step-3"),
        document.getElementById("step-4"),
    ];

    if (btn) {
        btn.addEventListener("click", () => {
            // @ts-ignore
            btn.disabled = true;
            // @ts-ignore
            btn.innerText = "PROCESANDO...";

            steps.forEach((s) => {
                s?.classList.remove("active", "done");
                s?.classList.add("pending");
            });

            let delay = 0;
            steps.forEach((step, index) => {
                setTimeout(() => {
                    step?.classList.remove("pending");
                    step?.classList.add("active");

                    setTimeout(() => {
                        step?.classList.remove("active");
                        step?.classList.add("done");
                    }, 1200);
                }, delay);
                delay += 1500;
            });

            setTimeout(() => {
                // @ts-ignore
                btn.disabled = false;
                // @ts-ignore
                btn.innerText = "Ejecutar Automatización";
            }, delay + 500);
        });
    }
</script>

<style>
    .automation-showcase {
        background: #0f0f0f;
        border: 1px solid #333;
        border-radius: 12px;
        max-width: 900px;
        margin: 40px auto;
        overflow: hidden;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.6);
        font-family: "Inter", sans-serif;
    }

    .automation-header {
        background: #1a1a1a;
        padding: 12px 20px;
        display: flex;
        align-items: center;
        border-bottom: 1px solid #333;
    }
    .window-controls {
        display: flex;
        gap: 8px;
        margin-right: 15px;
    }
    .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
    }
    .red {
        background: #ff5f56;
    }
    .yellow {
        background: #ffbd2e;
    }
    .window-title {
        font-size: 0.8rem;
        color: #888;
        font-family: monospace;
    }

    .automation-body {
        display: flex;
        padding: 30px;
        gap: 20px;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;
    }

    .col-input,
    .col-process {
        flex: 1;
        min-width: 280px;
    }
    .process-arrow {
        font-size: 2rem;
        color: #333;
    }

    .card-label {
        font-size: 0.7rem;
        color: var(--accent);
        letter-spacing: 1px;
        margin-bottom: 10px;
        font-weight: bold;
    }

    .email-mockup {
        background: #1e1e1e;
        padding: 15px;
        border-radius: 8px;
        border: 1px solid #333;
    }
    .email-head {
        display: flex;
        gap: 10px;
        margin-bottom: 15px;
        border-bottom: 1px solid #333;
        padding-bottom: 10px;
    }
    .avatar {
        width: 30px;
        height: 30px;
        background: #444;
        border-radius: 50%;
    }
    .meta {
        font-size: 0.75rem;
        color: #ccc;
    }
    .email-body {
        font-size: 0.8rem;
        color: #888;
    }
    .attachment-file {
        margin-top: 10px;
        background: #2a2a2a;
        padding: 8px;
        border-radius: 4px;
        color: #fff;
        font-size: 0.75rem;
        border: 1px dashed #555;
    }

    .step-list {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }
    .step {
        padding: 12px;
        background: #1a1a1a;
        border-radius: 6px;
        font-size: 0.85rem;
        color: #666;
        transition: 0.3s;
        border-left: 3px solid transparent;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .step.active {
        background: #1e293b;
        color: #fff;
        border-left-color: var(--primary);
        transform: translateX(10px);
    }
    .step.done {
        background: rgba(16, 185, 129, 0.1);
        color: var(--accent);
        border-left-color: var(--accent);
    }

    .action-area {
        padding: 20px;
        background: #0a0a0a;
        border-top: 1px solid #333;
        text-align: center;
    }
    .small-note {
        font-size: 0.75rem;
        color: #555;
        margin-top: 10px;
    }

    @media (max-width: 768px) {
        .process-arrow {
            transform: rotate(90deg);
            margin: 10px 0;
        }
        .automation-body {
            flex-direction: column;
        }
    }
</style>
